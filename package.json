{
  "name": "kronos",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"HUB,FINANCE,TREINO,TODO,DIETA\" --prefix-colors \"cyan,green,yellow,magenta,blue\" \"npm run dev --prefix hub\" \"npm run dev --prefix Finance\" \"npm run dev --prefix Treino\" \"npm run dev --prefix todolist\" \"npm run dev --prefix Dieta\"",
    "dev:hub":     "npm run dev --prefix hub",
    "dev:finance": "npm run dev --prefix Finance",
    "dev:treino":  "npm run dev --prefix Treino",
    "dev:todo":    "npm run dev --prefix todolist",
    "dev:dieta":   "npm run dev --prefix Dieta",
    "build": "npm run build --prefix hub && npm run build --prefix Finance && npm run build --prefix Treino && npm run build --prefix todolist && npm run build --prefix Dieta",
    "deploy": "npm run build && node deploy.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
